import { useNearcrowdContract, CONTRACT_ID } from './contract';

import {
    useWhitelistedContext,
    WhitelistedProvider
} from './WhitelistedContext';

export default {
    CONTRACT_ID,
    useNearcrowdContract,
    useWhitelistedContext,
    WhitelistedProvider
};
